@using Util.BootStrap.Study.HtmlPackage.Lesson2
@using Util.BootStrap.Study.HtmlPackage.Lesson2.BootStrapButton

@{
    ViewBag.Title = "Index";
    var x = Html.BootStrap();
}
<br />
@using (var group = x.ButtonGroup().Begin())
{
    @group.Button().Name("危险")
    <br />
    @group.Button().Name("危险").Size().Color()
    <br />
    @group.Button().Name("危险").Size(ButtonSize.Large).Color(ButtonColor.Danger)
    <br />
    @x.TextBox()
}

